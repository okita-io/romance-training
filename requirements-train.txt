# Train — local fine-tuning stack (torch, transformers, Unsloth-oriented pins)
# Install: pip install -r requirements-train.txt
# Full map: REQUIREMENTS.md
#
# Python 3.10+ required (Unsloth / peft>=0.18).

# Core ML frameworks
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.30.0
peft>=0.10.0
bitsandbytes>=0.43.0

# Training libraries (match unsloth-zoo; do not use trl<0.9 with current Unsloth)
trl>=0.18.2,<=0.24.0,!=0.19.0
# xformers: optional; install after torch if needed (version resolved by pip)

# Data handling
datasets>=2.18.0
tokenizers>=0.19.0
huggingface-hub>=0.22.0

# Utilities
packaging>=23.0
tqdm>=4.65.0
numpy>=2.1.3
sentencepiece>=0.2.0
tomli>=2.0.1; python_version < "3.11"

# Terminal training curves (optional; used when train_config report_to includes "cli")
cli-charts>=0.0.1

# Unsloth (installed separately via git)
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# --- Style pipeline dependencies ---

# Computable metrics (Phase 2)
spacy>=3.7.0
# After install: python -m spacy download en_core_web_sm
textstat>=0.7.3

# Ollama Python client — for LLM-based semantic metrics
# Requires Ollama running locally: https://ollama.ai
ollama>=0.1.0

# PDF → Markdown conversion (Phase 1 — extract_rubric.py only)
# Install separately (complex deps): pip install marker-pdf
# Or via: pip install "marker-pdf[full]"
# marker-pdf>=0.2.0

# PDF page rendering for vision harness (pdf_vision_harness.py)
pymupdf>=1.24.0

# Repo-root .env for OPENROUTER_API_KEY, LLM_* overrides
python-dotenv>=1.0
